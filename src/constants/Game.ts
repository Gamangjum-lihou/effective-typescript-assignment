export const INPUT = Object.freeze({
  number_length: 3,
  start: 1,
  end: 3
});

export const JUDGE = Object.freeze({
  ball: '볼',
  strike: '스트라이크',
  nothing: '낫싱'
});

export const COMMAND = Object.freeze({
  retry: '1',
  quit: '2'
});
